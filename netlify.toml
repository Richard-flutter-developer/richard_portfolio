[build]
  command = "bash ./install_flutter.sh && ./flutter/bin/flutter build web --release"
  publish = "build/web"

[build.environment]
  FLUTTER_CHANNEL = "stable"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
